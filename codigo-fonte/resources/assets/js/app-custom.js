/**
 * Apenas funções especificar e generalizadas para o PROJETO.
 * ----------------------------------------------------------
 * Objeto|Classe de formatação
 * @type object
 */
var appCustom = {
    
};

$(document).ready(function() {
    
});